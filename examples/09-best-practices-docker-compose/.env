# Przykładowe wartości zmiennych środowiskowych dla docker-compose.good.yaml
# Skopiuj ten plik jako `.env` i dostosuj do swoich potrzeb.

# Ogólne
APP_ENV=production
TZ=UTC

# Porty
APP_PORT=8000
WEB_PORT=8080

# Baza danych (Uwaga: NIE commituj prawdziwych haseł do repo!)
POSTGRES_USER=appuser
POSTGRES_PASSWORD=changeme
POSTGRES_DB=appdb
