services:
  app:
    build: .
    image: example/todo-spring-boot:latest
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default
    volumes:
      - todo-data:/data
    restart: unless-stopped

volumes:
  todo-data: {}
